# Required metadata
sonar.projectKey=aws_python
sonar.projectName=Python :: aws
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=

# Language
sonar.language=py

# Encoding of the source files
sonar.sourceEncoding=UTF-8
